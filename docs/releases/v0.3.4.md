# v0.3.4 — Editing workspace and reconnection / 편집 화면·원본 재연결

**Published and locally installed Windows preview.** Source/CI, final package and anonymous public-file verification passed. After saved-work confirmation, the local 0.3.4 upgrade preserved all 60 checked profile files byte-for-byte. The installed app was relaunched and its API reports 0.3.4. The [0.3.3 release](v0.3.3.md) remains available.

**Windows 프리뷰 공개·로컬 설치 완료.** 소스·CI·최종 패키지·공개 파일 검증을 통과했습니다. 작업 저장 확인 후 0.3.4를 설치했고 확인한 사용자 저장 파일 60개는 바이트 단위로 그대로 보존됐습니다. 설치 앱을 다시 열어 API 버전 0.3.4를 확인했습니다. [0.3.3 릴리즈](v0.3.3.md)도 보존합니다.

## Changes / 변경 사항

- **Independent panel and column widths.** Drag the right edge of project settings
  or the preview/cut panel to resize each separately. Caption time and speaker
  columns also resize independently; subtitle text uses the remaining width.
  Keyboard arrows/Home/End and reset controls are available. Preferences persist
  on this device; temporary viewport clamps do not overwrite them. Narrow
  workspaces retain their stacked layout.
  / **패널·열 너비 개별 조절:** 프로젝트 설정과 미리보기·컷 패널의 오른쪽 경계를
  각각 드래그합니다. 자막 표의 시간·인물 열도 따로 조절하며 자막은 나머지 폭을
  사용합니다. 방향키·Home/End·기본값 복원과 기기별 저장을 지원하고, 좁은 창의
  임시 제한이 저장한 폭을 덮어쓰지 않습니다. 좁은 화면에서는 세로 배치를 유지합니다.
- **Automatic video fit.** Preview uses the largest uncropped, aspect-preserving
  size that fits its measured width and available height after controls, cuts and
  notes. The old height slider and stored manual height no longer constrain it.
  Collapse, playback and fullscreen remain. Letterboxing already encoded in the
  original is not removed.
  / **영상 자동 맞춤:** 재생 조작·컷·메모가 차지한 공간을 뺀 실제 가로·세로 안에서
  원본 비율을 유지하며 최대 크기로 표시합니다. 이전 높이 슬라이더와 저장된 수동
  높이 제한은 적용하지 않습니다. 접기·재생·전체 화면은 유지하며 원본에 들어 있는
  검은 여백을 자르거나 영상을 늘리지 않습니다.
- **Usable notes.** Resize the notes panel vertically, retain that preference, and
  edit start/end times as seconds or timecodes. Set either boundary to the current
  playback position. Cards adapt to panel width, an empty panel stays compact,
  and explicitly opening a timeline note reveals its editor.
  / **메모 조작 개선:** 메모 패널 높이를 조절·저장하고 시작·종료를 초 또는 시간
  코드로 입력합니다. 현재 재생 위치를 시작·종료로 넣을 수 있습니다. 폭에 따라
  카드 배치를 바꾸고 빈 메모는 작게 표시하며 타임라인에서 편집을 열면 해당 메모를
  보여 줍니다.
- **Desktop original-media reconnection.** After a file is explicitly selected,
  the desktop app remembers its location privately on this device. Reopening its
  project verifies the original's SHA256 and byte count, then reuses a verified
  local media cache or streams a replacement upload to the local backend.
  Missing or changed originals require manual reconnection. Checking, connected
  and disconnected states have text/icons as well as color.
  / **설치형 원본 자동 재연결:** 직접 선택한 파일 위치를 이 기기에 별도로 기억합니다.
  프로젝트를 다시 열면 원본의 SHA256·크기를 확인한 뒤 검증된 로컬 캐시를 재사용하거나
  로컬 서버로 다시 스트리밍합니다. 파일이 없어졌거나 내용이 다르면 수동 재연결을
  안내합니다. 확인 중·연결됨·미연결은 색뿐 아니라 아이콘과 글자로 구분합니다.
- **Protect connected media during cache cleanup.** The cache list marks the
  currently connected desktop media as protected, excludes it from reclaimable
  space, and disables both individual and bulk deletion. Confirmations are
  rechecked against the current connection and refreshed cache list before
  submitting; files newly uploaded after the reviewed selection are not added
  to that cleanup.
  / **연결된 캐시 삭제 보호:** 현재 편집기에 연결된 설치형 미디어를 보호 표시하고
  정리 가능 용량에서 제외하며 단건·일괄 삭제를 막습니다. 확인 창을 연 뒤 원본 연결이나
  캐시 목록이 바뀌어도 전송 직전에 다시 검사합니다. 확인 대상으로 보지 않은 새 업로드를
  일괄 정리에 추가하지 않습니다.
- **Timeline item menu.** Right-click a caption or note, or use Shift+F10, to open
  its editor, seek, move it to the playback position, or delete it. Moving preserves
  duration and clamps to media boundaries; project edits use existing undo.
  Opening the menu or editor does not itself start playback.
  / **타임라인 항목 메뉴:** 자막·메모에서 우클릭 또는 Shift+F10으로 편집 열기,
  해당 시점 이동, 현재 위치로 옮기기, 삭제를 선택합니다. 옮기기는 길이·미디어 끝
  경계를 지키며 변경은 실행 취소할 수 있습니다. 메뉴나 편집기를 여는 것만으로
  재생하지 않습니다.
- **Sharper dense timelines.** Dense lanes draw only the visible area and a small
  margin at the display's pixel ratio, instead of stretching a capped bitmap
  across the whole zoomed timeline. Horizontal scrolling, click targeting and
  context menus retain their position; clicking a dense lane no longer jumps the
  scroll before computing the selected time.
  / **밀집 타임라인 선명도·클릭 수정:** 확대된 전체 폭에 작은 비트맵을 늘리는 대신
  보이는 구간과 짧은 여유 영역만 화면 배율에 맞춰 그립니다. 가로 스크롤·클릭·메뉴
  위치를 유지하며, 클릭 순간 스크롤이 왼쪽으로 이동해 엉뚱한 시간을 선택하던
  문제를 수정했습니다.
- **Stable progress panels.** Queue controls and job details now have distinct
  sibling keys. Repeated polling no longer accumulates duplicate controls or
  resets a pending confirmation; changing jobs still resets the correct state.
  / **진행창 중복 방지:** 대기열 조작과 작업 상세의 내부 키를 분리했습니다. 반복
  상태 확인 때 조작 영역이 쌓이거나 확인 화면이 초기화되는 문제를 수정하며,
  다른 작업으로 전환할 때 필요한 상태 초기화는 유지합니다.
- **Visible VST actions and stale-preview cleanup.** Changing the input media,
  track or chain invalidates its comparison request and releases the preview
  lock. Late responses cannot overwrite a newer comparison/editor. Native editor
  progress and errors appear beside the clicked slot; a pre-request snapshot
  failure is shown rather than becoming an unhandled click.
  / **VST 무반응 경로 수정·즉시 표시:** 비교 생성 중 원본·트랙·체인이 바뀌면 이전
  비교 요청과 잠금을 정리합니다. 늦은 응답은 새 비교·설정창을 덮어쓰지 않습니다.
  클릭한 슬롯 옆에 전용 창 진행 상태·오류를 표시하고, 요청 준비 중 오류도 화면에
  알립니다. 사용자 설치본에서 발생한 모든 무반응의 원인을 확정한 것은 아닙니다.

- **Creator links.** The header logo and “Created by LiveTrack” footer open the
  creator's [GitHub profile](https://github.com/LiveTrack-X).
  / **제작자 링크:** 상단 로고와 하단 “Created by LiveTrack”에서 제작자 GitHub
  프로필을 열 수 있습니다.

## Verification completed / 완료한 검증

Machine-readable evidence / 구조화한 검증 기록: [v0.3.4 validation](../evidence/v0.3.4-validation.json).

| Check / 검사 | Result / 결과 | Boundary / 범위 |
| --- | --- | --- |
| Frontend and desktop scripts / 프런트·데스크톱 스크립트 | 843 Vitest + 84 Node tests passed / 통과 | Full `npm test`; source regression, not installation proof / 소스 회귀·설치 증거 아님 |
| Backend / 백엔드 | 1,023 passed, 1 skipped / 통과·건너뜀 | The skipped diagnostics test needs Windows symlink permission / Windows 심볼릭 링크 권한이 필요한 진단 검사 1개 |
| TypeScript and production web build / 타입·웹 빌드 | Passed / 통과 | Vite retains a large-chunk warning / 큰 번들 경고는 남음 |
| Frozen backend / 동결 백엔드 | 42 application modules matched source; offline health and clean shutdown passed / 앱 모듈 42개 소스 일치·오프라인 상태·정상 종료 통과 | Whisper/Nemotron/FFmpeg discovery checked; **no new inference run** / 실행환경 확인이며 **새 추론 실행 아님** |
| Workspace browser QA / 화면 실제 조작 | Passed with synthetic video, 120 captions and 2 notes / 합성 영상·자막 120개·메모 2개 통과 | Pointer/keyboard resizing, reload persistence, note time/edit/delete/undo, contain fit, narrow 950px layout without horizontal overflow / 크기·저장 복원·메모·자동 맞춤·950px 가로 넘침 없음 |
| Desktop media reconnection / 설치형 원본 재연결 | Temporary backend + 1-second synthetic WAV passed / 임시 서버·1초 합성 WAV 통과 | Cache reuse, HTTP Range 206, streamed replacement after cache eviction, matching hash, changed/missing-file rejection, registry reuse after service recreation. Native file-picker→app restart UI not verified / 캐시·부분 재생·재업로드·해시·변경/누락 거부·서비스 재생성, 네이티브 파일 선택→앱 재시작 전체 UI는 미검증 |
| Connected cache protection / 연결 캐시 보호 | 53 focused checks and real React DOM scenarios passed / 집중 검사 53개·실제 React DOM 시나리오 통과 | Connected single/bulk deletion blocked, reclaimable bytes corrected, stale confirmations rechecked, later uploads excluded. Synthetic fixtures only / 연결 항목 단건·일괄 보호·용량·오래된 확인 재검사·새 업로드 제외, 합성 자료만 사용 |
| Dense timeline browser QA / 밀집 타임라인 실제 조작 | Passed at 8× zoom and display ratio 1.25 / 8배 확대·화면 배율 1.25 통과 | Synthetic dense/sparse lanes, scrolling, click/context targeting and boundary control; helper tests additionally cover ratios 1/1.5/2/3 / 합성 밀집·일반 트랙·스크롤·클릭·경계, 다른 배율은 함수 검사 |
| Polling reconciliation / 진행창 갱신 | 24 updates retained one control panel, confirmation and correct job reset / 24회 갱신에도 조작 영역 1개·확인 상태·작업 전환 유지 | Hidden Electron with synthetic responses; no analysis cancellation / 실제 Electron·합성 응답, 사용자 분석 중단 없음 |
| VST click regression / VST 클릭 회귀 | Reproduced before the fix and passed after; 5 new lifecycle cases / 수정 전 재현·수정 후 통과, 신규 수명주기 검사 5개 | Isolated real component with mocked API; **no new native plug-in execution** / 실제 컴포넌트·모의 API, **새 네이티브 플러그인 실행 아님** |

The [versioned validation record](../evidence/v0.3.4-validation.json) records the
completed checks separately from release and installation status.
[버전별 검증 기록](../evidence/v0.3.4-validation.json)은 완료한 검사와 게시·설치 상태를 구분합니다.

Local evidence: `tmp/v0.3.4-frontend-tests-final.log`,
`tmp/v0.3.4-backend-tests.log`, `tmp/v0.3.4-frozen-source-check.json`,
`tmp/v0.3.4-runtime-check.json`, `tmp/v0.3.4-workspace-browser-check.json`,
`tmp/timeline-density-validation.json`, `tmp/v0.3.4-analysis-reconciliation.json`,
`tmp/v0.3.4-vst-click-qa.json`, `tmp/v034-relink-integration/result.json` and
`tmp/v0.3.4-cache-protection-validation.json`. These are local validation artifacts, not public
release downloads. Final package hashes, CI, anonymous download/signature checks
and installation results are recorded below and in the structured evidence.

위 `tmp` 파일은 로컬 검증 자료이며 공개 다운로드 파일이 아닙니다. 최종 패키지 해시,
CI, 비로그인 다운로드·서명 확인과 설치 결과는 아래와 구조화한 검증 기록에 남겼습니다.

## Publication / 공개 배포

[GitHub release](https://github.com/LiveTrack-X/VOICESUBSEP/releases/tag/v0.3.4) published at `2026-09-25T14:25:04Z`. Tag `v0.3.4` points to `f6f0d514b4e8462ee9898e4725e92b4cd4b7476a`; [tag CI](https://github.com/LiveTrack-X/VOICESUBSEP/actions/runs/36146198381) and [main CI](https://github.com/LiveTrack-X/VOICESUBSEP/actions/runs/36146197636) passed.

All 10 remote asset sizes and GitHub SHA256 digests match the local release. Anonymous downloads of the manifest, signature, checksums and online EXE match local bytes, and Ed25519 verification passed. The updater offers 0.3.4 to 0.3.3 and reports no update for 0.3.4. The full 2.39 GB public payload was not downloaded again, and downloaded executables were not run.

파일 10개의 원격 크기·GitHub SHA256이 로컬과 같습니다. 명세·서명·체크섬·온라인 EXE를 로그인 없이 받아 바이트·서명을 검증했습니다. 0.3.3에서 0.3.4를 찾고 0.3.4에서는 추가 업데이트가 없음을 확인했습니다. 공개 2.39GB 데이터 전체를 다시 받거나 다운로드한 EXE를 실행한 검증은 아닙니다. Windows 실행 파일은 무서명이며 명세 서명과 Authenticode는 다릅니다.

## Verified local artifacts / 검증된 로컬 배포 파일

Application source: `9447dc13e2b63325c21f490e1e5cf419054c8660`,
[CI passed](https://github.com/LiveTrack-X/VOICESUBSEP/actions/runs/36145258833).
Final packaging, offline packaged-backend startup/shutdown, source comparisons,
archive CRC and all 114,110 embedded blockmap chunks passed. The 4,392-file backend
inventory matches the build, and the package includes no project/session data or
Whisper/Nemotron weights. The online installer passed 15 tests and its UI smoke.

최종 패키지의 소스 일치·오프라인 백엔드 실행/종료·압축 CRC·내장 블록맵
114,110개 검사를 통과했습니다. 백엔드 파일 4,392개 목록이 빌드와 같고 프로젝트·세션
자료 및 Whisper/Nemotron 가중치는 포함하지 않습니다. 온라인 설치기 검사 15개와
UI 실행 검사도 통과했습니다. 공개 다운로드·사용자 프로필 설치는 별도 단계입니다.

| Artifact / 파일 | Bytes / 크기 | SHA256 |
| --- | ---: | --- |
| Online Setup EXE | 166,912 | `c2dba115b0ae0bad9ef9361d193bb7c03d97191c12f72eaadb5d27808ee00b73` |
| Offline Setup EXE | 1,045,608 | `070dd57c2af3349a892588ba2e2c5b575047ebff736968359b32791203c785bd` |
| Reassembled payload / 재조립 파일 | 2,394,655,120 | `4f78c6bf8f66046c1ba9399fc21881ac69ea05fb7291ff5173b88e524ac697d1` |
| Part 001 | 1,073,741,824 | `fca424c6e62ef3dbb5f4115c586d5d2bd92f0646f88c38a6c055e1f667947a43` |
| Part 002 | 1,073,741,824 | `bb02256213d77fac416f473cfce36e0182fe3cd3bc5f13216e4bbcb69f4c0281` |
| Part 003 | 247,171,472 | `d62c474d3b5309a239817b6c56ab564cafbe1d131c908589bed59b53501172a6` |

Packaged backend SHA256: `1b32e5894ad29b77584995cb4641a1e7497fdd74b2260c0335c4f760757e5126`.
Desktop ASAR SHA256: `1966ed74434d762a4fc38804ce4679503faea6ca59b0a34c004b9fa47a08d0dc`.

## Local installation / 로컬 설치

After the user confirmed saved work, the verified offline installer exited 0 and
installed version 0.3.4.0. All 60 checked profile files were unchanged immediately
after installation, before relaunch. The executable, ASAR, backend executable and
all web files (10 files total) match the final package. The installed backend's
isolated offline readiness and clean-shutdown check passed without running
inference. The normal app was reopened; its visible window is responsive, titled
VOICESUBSEP, and its owned backend reports API version 0.3.4.

작업 저장 확인을 받은 뒤 검증된 오프라인 설치기가 종료 코드 0으로 0.3.4.0을
설치했습니다. 재실행 전 확인한 사용자 저장 파일 60개는 모두 변경되지 않았습니다.
앱 EXE·ASAR·백엔드 EXE·웹 파일 총 10개의 해시가 최종 패키지와 일치합니다.
설치된 백엔드의 격리 오프라인 실행·정상 종료 검사를 통과했으며 추론은 실행하지
않았습니다. 앱 창을 다시 열어 VOICESUBSEP 제목·응답 상태와 API 0.3.4를 확인했습니다.

## Limits and next work / 한계·다음 작업

Automatic original reconnection is **desktop-only and device-local**. It does not
restore a browser `File` after page reload, search the disk for moved files, embed
absolute paths in portable project exports, or guarantee access after a drive or
permission change. Old projects with no remembered location need one explicit
reconnection. Large files require a streamed hash check and may take time. The
service integration passed with temporary fixtures; the complete native file
selection, app restart and restored-playback UI path has not yet been verified.

원본 자동 연결은 **설치형 앱·같은 기기** 범위입니다. 브라우저 새로고침 후 `File`
복원, 이동한 파일의 디스크 검색, 프로젝트 내보내기에 절대 경로 포함, 드라이브·권한
변경 후 접근 보장은 제공하지 않습니다. 위치 기록이 없는 기존 프로젝트는 한 번
직접 연결해야 하며, 큰 파일의 스트리밍 해시 확인에는 시간이 걸릴 수 있습니다.
임시 합성 파일의 서비스 통합 검사는 통과했지만 네이티브 파일 선택→앱 재시작→자동
재연결 재생의 전체 UI 경로는 아직 검증하지 않았습니다.

Connected-media cache protection applies to this app's cleanup UI. It does not
introduce a server-side playback lease: another API client or app instance can
still delete a cache entry that the backend otherwise considers unused. No real
user cache or project was deleted during verification.

연결 미디어 보호는 이 앱의 캐시 정리 UI 범위입니다. 재생 중인 파일을 서버에서 모든
클라이언트에 걸쳐 예약하는 기능은 아니므로, 다른 API 클라이언트나 앱 인스턴스가
서버 기준으로 미사용인 캐시를 삭제하는 것까지 막지 않습니다. 검증 중 실제 사용자
캐시나 프로젝트를 삭제하지 않았습니다.

This release does not introduce a new speaker-assignment model or claim fewer
unassigned cues or better recognition accuracy. The
[speaker-assignment strategy](../SPEAKER-ASSIGNMENT-STRATEGY.md) is a **proposal**,
not an implemented algorithm. Existing Whisper/Nemotron, overlap and review
limitations remain. Subtitle translation and AI/Ollama meeting summaries remain
removed. Native VST support still depends on separately installed compatible
plugins and their licenses.

새 화자 배정 모델, 미배정 감소, 인식률 향상을 이번 변경의 성과로 주장하지 않습니다.
[미배정 감소 전략](../SPEAKER-ASSIGNMENT-STRATEGY.md)은 **제안**이며 알고리즘에 아직
적용하지 않았습니다. 기존 인식·동시 발화·검수 한계와 번역·AI/Ollama 자동 요약 제거는
유지합니다. 전용 VST 창은 별도 설치된 호환 플러그인·라이선스가 필요합니다.

See [feature status](../FEATURE-STATUS.md), [user guide](../USER-GUIDE.md),
[desktop installation](../DESKTOP.md) and [dependency notices](../BUNDLED-NOTICES.md).
