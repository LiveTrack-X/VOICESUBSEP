# VOICESUBSEP 0.3.3 — Release preparation / 릴리즈 준비

**Implemented in source; publication and installation are pending.** The public
download remains [v0.3.2](v0.3.2.md). This draft does not establish a final release
commit, artifact hashes, CI acceptance or a completed installer upgrade. Those
checks are in progress and will be recorded separately. The published 0.3.2
release, tag and evidence are preserved.

**소스 구현을 마쳤으며 게시와 설치는 아직 진행 전입니다.** 현재 공개 다운로드는
[v0.3.2](v0.3.2.md)입니다. 이 초안은 최종 릴리즈 커밋·파일 해시·CI·설치 완료를
뜻하지 않습니다. 해당 검증은 진행 중이며 별도로 기록합니다. 공개 0.3.2 릴리즈·태그·
당시 증거는 보존합니다.

## Changes / 변경 내용

| English | 한국어 |
| --- | --- |
| Optional built-in RNNoise uses CPU FFmpeg `arnndn` with the pinned 302,903-byte Xiph RNNoise v0.1 model and original notices. Default off; enabling starts at 70% processed mix. It works without VST and precedes VST when combined. ASR-only remains default, with optional diarization scope and original/processed A/B preview. | CPU FFmpeg `arnndn`과 고정 302,903바이트 Xiph RNNoise v0.1 모델·원본 고지를 사용하는 선택 필터입니다. 기본 꺼짐, 켜면 처리음 70%로 시작합니다. VST 없이 작동하고 함께 쓰면 RNNoise→VST 순서입니다. 기본 인식만·선택 화자 포함·원본/처리음 비교를 제공합니다. |
| RNNoise compensates a fixed 480-sample/10 ms algorithm delay and verifies exact original sample count. Failed, cancelled or invalid outputs do not replace an existing result or modify the source. No runtime model download is needed. | RNNoise 고정 480샘플·10ms 지연을 보정하고 원본 샘플 수와 정확히 같은지 검사합니다. 실패·취소·잘못된 출력은 원본이나 기존 결과를 덮어쓰지 않습니다. 실행 중 모델 다운로드는 필요 없습니다. |
| Native Windows VST3 editors open in isolated workers. Close/apply stores exposed parameters and up to 256 KiB of opaque state per slot; cancel discards changes. Saved state is restored before explicit parameter overrides. | Windows VST3 전용 창을 격리 작업 프로세스에서 엽니다. 닫고 적용하면 노출 매개변수와 슬롯당 최대 256KiB 내부 상태를 저장하고 취소하면 변경을 버립니다. 내부 상태 복원 뒤 직접 지정한 매개변수를 적용합니다. |
| An owned-window helper keeps the native editor/title bar in the work area when possible. The app supplies an explicit close action. Universal GUI scaling is unavailable; use a vendor's own zoom menu when supplied. | 가능한 범위에서 소유한 전용 창·제목 표시줄을 화면 작업 영역에 배치하고 앱 쪽 닫기 동작을 제공합니다. 범용 GUI 배율은 없으며 제조사가 제공하는 자체 배율 메뉴를 사용합니다. |
| Transient Windows response-file locks use bounded retries. Missed intermediate progress no longer aborts processing; final response failure remains an explicit error. | Windows 상태 파일의 일시 잠금은 제한된 재시도로 처리합니다. 중간 진행 정보 저장 실패만으로 처리를 중단하지 않되 최종 응답 저장 실패는 명시 오류로 남깁니다. |
| Electron microphone checks now allow audio-device names after explicit approval, retain that grant only for the current document, and reject camera/foreign-frame access. Probe streams stop immediately; selector-adjacent status distinguishes hidden labels, no devices and actual permission errors. | 명시 승인 뒤 Electron 오디오 장치 이름을 읽을 수 있도록 수정하고 현재 문서에서만 승인을 유지합니다. 카메라·외부 프레임은 거절합니다. 확인용 스트림은 즉시 해제하고 이름 숨김·장치 없음·실제 권한 오류를 선택기 근처에서 구분합니다. |
| F1 opens a searchable shortcut hub with personal bindings, conflict/reserved-key checks and restore defaults. Space play/pause, undo/redo, five-second seeks and caption navigation respect text input, IME, dialogs and native button activation. | F1 단축키 허브에서 검색·개인 지정·충돌/예약 키 검사·기본값 복원을 제공합니다. Space 재생, 실행 취소/다시 실행, 5초·자막 이동은 입력·한글 조합·대화상자·버튼 고유 동작을 보호합니다. |
| Playback follow scrolls the active caption without changing selected cues, filters or playback; manual interaction and editing suspend it. Cuts use readable IN/OUT timecodes, range preview and a separate exclusion-review step. | 재생 따라가기는 선택 자막·필터·재생 시점을 바꾸지 않고 현재 자막으로 스크롤하며 직접 조작·편집 중 멈춥니다. 컷은 IN/OUT 시간 입력·구간 재생·제외 목록 검토를 구분합니다. |
| Speech-language selection is emphasized independently of interface language. A visible reset action restores local large-v3 + Nemotron; it does not silently replace saved engine choices. The window title is simply VOICESUBSEP. | 화면 언어와 음성 인식 언어를 분명히 구분합니다. 기본 조합 복원 버튼은 로컬 large-v3+Nemotron으로 되돌리며 저장한 엔진 선택을 임의로 바꾸지 않습니다. 창 제목은 VOICESUBSEP만 표시합니다. |

## Verification so far / 현재까지의 검증

- **RNNoise module:** 41 tests passed, including actual FFmpeg mono/stereo
  processing at 1, 479, 480, 481 and 48,017 frames, sample-exact dry mix, zero
  residual correlation lag for a synthetic wet fixture, original preservation,
  model tampering, incomplete output, cancellation and child-process reaping.
  Separate integration checks cover processing order and ASR/diarization scope.
  / **RNNoise 모듈:** 실제 FFmpeg 길이·채널·강도 0 샘플 일치·합성 처리음 잔여 지연 0,
  원본 보존·모델 변조·불완전 출력·취소·자식 회수 등 41개 통과. 순서·적용 범위는
  별도 통합 검사로 확인합니다.
- **Native VST GUI:** installed CLEAR opened successfully; close/apply returned
  17 parameters and 1,067 bytes of state. Restoring then cancelling returned no
  applicable result. Local evidence: `tmp/v0.3.3-native-editor-smoke.json`.
  Window-position helper tests passed; actual placement on every monitor/plugin
  has not been visually accepted. / **전용 창:** 설치된 CLEAR 열기·닫고 적용, 17개
  매개변수·1,067바이트 상태, 복원 후 취소 시 적용 결과 없음 확인. 위치 보조의 모든
  모니터·플러그인 육안 수용 검증은 아닙니다.
- **Microphone permission:** 11 desktop permission tests passed and a real
  hidden Electron instance with fake Chromium devices demonstrated hidden labels
  before approval, three named devices afterward, exact fixed-device selection,
  stopped probe tracks, repeated explicit prompts and grant revocation on reload.
  No physical microphone or system audio was recorded. Local evidence:
  `tmp/microphone-permission-validation.json`. / **마이크 권한:** 11개 데스크톱 검사와
  가짜 장치 실제 Electron에서 승인 전후 이름·고정 장치 선택·확인 스트림 해제·
  재승인·새로고침 무효화 확인. 실제 사용자 마이크·시스템 소리는 녹음하지 않았습니다.
- **Packaging inputs:** PyInstaller data collection finds the RNNoise model,
  unchanged upstream `COPYING` and provenance README. The build checks copied
  hashes. Full regression, frozen package, installer and public-download checks
  are **in progress**, not inferred from these focused checks. / **패키징 입력:**
  모델·원본 고지·출처 README 수집과 복사 해시 검사를 연결했습니다. 전체 회귀·동결
  패키지·설치·공개 다운로드는 **검증 중**이며 개별 검사로 완료를 대신하지 않습니다.

See the [validation record](../evidence/v0.3.3-validation.json) for completed checks and pending release stages.
The `tmp` paths above are local test artifacts, not released or tracked files.
[검증 기록](../evidence/v0.3.3-validation.json)은 완료 검사와 대기 중인 배포 단계를 구분합니다. 위 `tmp` 경로는 로컬 검사 자료이며
배포·버전 관리 파일이 아닙니다.

## Limits and compatibility / 한계와 호환

Preprocessing remains file-analysis/preview only: it does not modify originals,
final-render audio or live capture. Denoising may damage quiet speech, laughter
or overlapping voices; listening improvements do not establish ASR accuracy.
Real user microphones/interfaces, long recordings and all VST products have not
been accepted by these tests. Commercial plugins remain separately installed
and licensed. Opaque VST state may contain sensitive vendor data; review settings
backups before sharing. Older versions may not accept new RNNoise/state settings;
retain a pre-upgrade settings backup if rollback is needed.

전처리는 파일 분석·비교용이며 원본·최종 렌더·라이브 음원을 바꾸지 않습니다. 작은
발화·웃음·겹친 말을 손상할 수 있고 듣기 개선이 인식률 향상을 입증하지 않습니다.
실사용 마이크·오인페·장시간 녹음·모든 VST 제품의 수용 검증은 남아 있습니다. 상용
플러그인은 별도 설치·라이선스가 필요하고 내부 상태에 민감한 제조사 정보가 들어갈 수
있으므로 설정 백업 공유 전 확인하세요. 옛 버전이 새 RNNoise/상태 설정을 읽지 못할 수
있으므로 복귀가 필요하면 업그레이드 전 설정을 보관합니다.

Subtitle translation and automatic AI/Ollama summaries remain removed. Default
analysis remains local Whisper + Nemotron. Whisper/Nemotron weights are separate;
the small RNNoise model and existing Silero VAD assets do not change that policy.
Windows code-signing status and final artifact identity must be reported with
the eventual release evidence, not assumed from an updater manifest signature.

사용자 결정으로 제거한 자막 번역·AI/Ollama 자동 요약은 복원하지 않습니다. 기본 분석은
로컬 Whisper+Nemotron입니다. Whisper/Nemotron 가중치는 별도이며 작은 RNNoise 모델과
기존 Silero VAD 포함이 그 정책을 바꾸지 않습니다. Windows 코드 서명 상태·최종 파일
식별 정보는 실제 릴리즈 증거에서 확인하며 업데이트 명세 서명으로 추정하지 않습니다.

See [user guide](../USER-GUIDE.md), [preprocessing details](../VST-CHAIN.md),
[feature status](../FEATURE-STATUS.md), and [dependency notices](../BUNDLED-NOTICES.md).
사용법·세부 처리·남은 기능·재배포 고지는 위 문서를 따릅니다.
