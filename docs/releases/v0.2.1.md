# VOICESUBSEP v0.2.1 Windows Preview

This patch makes subtitle editing usable in narrow windows. Wrapped toolbars previously consumed a fixed-height panel and left almost no visible caption rows. The editor now reserves at least 250 px for the caption list, lets the surrounding layout grow, and collapses editing notes by default on narrow screens.

좁은 창에서 자막 편집이 막히던 배치를 수정합니다. 이전에는 줄바꿈된 도구 모음이 고정 높이를 모두 차지해 자막 행이 거의 보이지 않았습니다. 자막 목록에 최소 250px을 확보하고 주변 높이를 자동으로 맞추며, 좁은 화면의 편집 메모는 기본 접힘으로 표시합니다.

- Notes remain available through expand/collapse, and selecting a timeline note opens its card. Note cards stack vertically without horizontal overflow. / 메모 펼침·접기와 타임라인 메모 선택은 유지하며, 카드는 가로 넘침 없이 세로로 표시합니다.
- The desktop window can shrink to 480 px wide for side-by-side editing. / 설치형 앱 창도 너비 480px까지 줄여 나란히 편집할 수 있습니다.
- Projects, captions, notes, names, and saved layout preferences are not migrated or cleared by this fix. / 이 수정은 프로젝트·자막·메모·이름·기존 레이아웃 저장 값을 삭제하거나 변환하지 않습니다.

## Download / 다운로드

Use [VOICESUBSEP-0.2.1-Online-Setup-x64.exe](https://github.com/LiveTrack-X/VOICESUBSEP/releases/download/v0.2.1/VOICESUBSEP-0.2.1-Online-Setup-x64.exe). It downloads and verifies the required app files before opening setup. No GitHub login is required. See the [English/Korean installer guide](../ONLINE-INSTALLER.md).

위 EXE 하나를 실행하면 필요한 앱 파일을 다운로드·검증한 뒤 설치기가 열립니다. GitHub 로그인은 필요하지 않습니다. 자세한 절차는 [한·영 설치 안내](../ONLINE-INSTALLER.md)를 확인하세요.

## Artifact and validation scope / 산출물·검증 범위

The app shell and frontend are version 0.2.1. The unchanged Python/CUDA backend is reused from the verified v0.2.0 bundle and continues to report API version 0.2.0. No new model download or GPU inference is required for this layout patch. The original v0.2.0 tag and release files are preserved.

앱 셸·화면 버전은 0.2.1입니다. Python·CUDA 백엔드는 검증된 v0.2.0 번들을 그대로 사용하여 API 버전은 0.2.0을 표시합니다. 화면 배치 수정 때문에 모델을 다시 받거나 GPU 추론을 실행하지 않습니다. 기존 v0.2.0 태그·파일은 보존합니다.

This remains an unsigned preview with no configured in-app update feed. Models and external VST plugins are separate. Source notices and dependency versions are unchanged; see [bundled notices and source locations](../BUNDLED-NOTICES.md). Full speech accuracy, real-device long recording and overlapping-speech recovery remain outside this patch's validation.

코드 서명과 인앱 업데이트 feed가 없는 Preview이며 모델·외부 VST는 별도입니다. 포함 고지와 의존성 버전은 같으며 [고지·원본 소스 위치](../BUNDLED-NOTICES.md)를 확인할 수 있습니다. 인식 정확도·실기기 장시간 녹음·겹친 발화의 복원 품질은 이번 화면 수정의 검증 범위에 포함하지 않습니다.
